extends layout

block content
    h1(style="text-align: center") #{title}
    .row
        each blog, i in blogs    
            .col.s12.m6
                .card.blue-grey.darken-1
                    .card-content.white-text
                        span.card-title= blog.title
                        p= blog.description
                    .card-action
                        a(href="/blog/"+blog._id) Read More